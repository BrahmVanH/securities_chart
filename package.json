{
	"name": "journal-logger",
	"version": "1.0.0",
	"description": "",
	"scripts": {
		"dev": "cd client && npm run build && netlify dev",
		"install": "cd client && npm i",
		"seed": "cd server && npm run seed",
		"build": "cd client && npm run build",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"devDependencies": {
		"concurrently": "^8.2.2"
	},
	"keywords": [],
	"author": "",
	"license": "MIT",
	"dependencies": {
		"typescript": "^5.2.2"
	}
}
